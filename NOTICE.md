# Licensing overview

This is a multi-license repository:

* **Code** (`importers/`, `validators/`, `scripts/`, `schema/v1/*.ts`):
  GNU Affero General Public License v3 only (`AGPL-3.0-only`) — see `LICENSE`.
* **Data** (`data/cities/`, `overrides/`, `topology/`, approved community
  changes): Open Database License v1.0 (`ODbL-1.0`) — see `DATA_LICENSE.md`
  and `LICENSES/ODbL-1.0.txt`.
* **Upstream source material** (`upstream-*/` submodules): their respective
  licenses (Tehran: ODbL-1.0). Never modified here — see `ATTRIBUTION.md`.

The AGPL on tooling does not override the ODbL on data outputs, and the ODbL
on data does not relicense the tooling.
