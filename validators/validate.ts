// Placeholder: dataset integrity checks (see validators/README.md for the rule list).
// Implemented in the build milestone: load data/cities/<city>/*.json and return string[] errors.
// Must exit non-zero on any error; must never read upstream-* directly (importer output only).
export {};
