#!/usr/bin/env node
// Scaffold: validate built dataset + contributions/pending/*.json against
// contributions/schema.json. Read-only; exits non-zero on any error.
console.log("validate: not yet implemented (see validators/README.md)");
process.exit(2);
