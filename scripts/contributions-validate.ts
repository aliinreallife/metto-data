#!/usr/bin/env node
// Scaffold: validate contributions/pending/*.json (schema + city/entity/path allowlist).
// Must not mutate data/cities/* or overrides/*.
console.log("contributions-validate: not yet implemented (see contributions/README.md)");
process.exit(2);
